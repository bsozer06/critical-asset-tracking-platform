name: CD

on:
  push:
    branches: [main]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # - name: Build and push backend image
      #   run: |
      #     docker build -t ghcr.io/${{ github.repository }}/backend:latest -f backend/Api/Dockerfile .
      #     docker push ghcr.io/${{ github.repository }}/backend:latest

      # - name: Build and push frontend image
        # run: |
        #   docker build -t ghcr.io/${{ github.repository }}/frontend:latest -f frontend/critical-asset-frontend/Dockerfile .
        #   docker push ghcr.io/${{ github.repository }}/frontend:latest

      # - name: Build and push simulator image
        # run: |
        #   docker build -t ghcr.io/${{ github.repository }}/simulator:latest -f simulator/CriticalAssetSimulator/Dockerfile .
        #   docker push ghcr.io/${{ github.repository }}/simulator:latest

  # Optional: Add deployment steps here (e.g., SSH, Azure, AWS, etc.)
